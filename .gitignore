# Private location data — personal city-level footprints. Never publish.
# The public footprints.html only contains aggregated per-country counts.
citylist.md
footprints-cities.private.json
*.private.json

# Editor backups
*.~undo-tree~

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
